# Notion integration (free) - see README.md "Notion setup" section for steps
# NOTION_API_KEY is shared across NoBakwaas + ELYNE (same integration
# token can access multiple pages) - each business has its own page ID.
NOTION_API_KEY=
NOBAKWAAS_NOTION_PAGE_ID=
